#ifndef PRIMES_CALCULATE_PRIMES_HPP
#define PRIMES_CALCULATE_PRIMES_HPP

#include <cstdint>
#include <vector>

namespace primes {

std::vector<std::uintmax_t> calculate_primes_until(std::uintmax_t);


} // namespace primes

#endif
